export enum SupervisorReason {
  CONFLICT = 'Employee is needed for major projects this year, which may conflict with CORE’s deployments.',
  NEGATIVE = 'Employee was burned out/had a negative experience in CORE from the previous year.',
  PERFORMANCE = 'Employee needs to undergo performance evaluation.',
  OTHER = 'Other',
}
