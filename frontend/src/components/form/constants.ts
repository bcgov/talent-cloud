export enum FieldTypes {
  TEXT = 'text',
  SELECT = 'select',
  CHECKBOX = 'checkbox',
  TOGGLE = 'toggle',
  RADIO = 'radio',
  MULTI = 'multi-select',
  SEARCH = 'search',
}

export const SelectAll = {
  value: '*',
  label: 'Select All',
};
