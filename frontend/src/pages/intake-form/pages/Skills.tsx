import { useFormikContext } from "formik";

export const Skills = () => {
  const {values, handleChange} = useFormikContext();  
  return(
  <>
 {/* Language */}
 {/* Tools */}
 {/* Certifications */}
  </>)
}